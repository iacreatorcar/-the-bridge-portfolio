---
// --- FRONTMATTER: Logica e Variabili ---
const name = "Carmine D'alise";

const quickLinks = [
  { name: "Main Portfolio", url: "https://carmine-dalise-portfolio.vercel.app/", icon: "📂" },
  { name: "HotelFlow Manager", url: "https://hotelflow-manager.vercel.app/index.html", icon: "🏨" },
  { name: "Cruise CMS (Emergent)", url: "https://sea-services.emergent.host", icon: "🚢" }
];

// --- NUOVA VARIABILE: TECH STACK ---
const techStack = [
  {
    category: "Full-Stack & Web Dev",
    it: "Sviluppo Web e App Moderne",
    items: ["React.js", "Astro", "HTML5/CSS3", "JS/JSON", "SQL", "Python", "C++", "Vercel"],
    icon: "💻"
  },
  {
    category: "Enterprise & CMS",
    it: "Gestione Contenuti e Pagamenti",
    items: ["Otallo", "Sanity CMS", "Stripe", "SAP UI", "Enterprise CMS", "API Integration"],
    icon: "🏢"
  },
  {
    category: "Cloud, AI & DevOps",
    it: "Infrastruttura e Automazione",
    items: ["Azure", "Docker", "GitHub", "Relevance AI", "Mesh", "Discord Bots"],
    icon: "☁️"
  },
  {
    category: "IT Infrastructure & On-Board",
    it: "Networking e Sistemi di Bordo",
    items: ["Cisco Tracer", "NAS Procedures", "Digital Signage", "Touch Surface", "Linux/Windows", "iOS/Android"],
    icon: "⚓"
  },
  {
    category: "Design & UI/UX Strategy",
    it: "Strategia Visiva e Grafica",
    items: ["Figma", "Adobe Family", "Canva", "UI/UX Strategy", "Visual Studio Code"],
    icon: "🎨"
  }
];

// Mantieni le costanti certifications, cityTours e digitalProcedures del codice precedente...
const certifications = [ /* ... dati precedenti ... */ ];
const cityTours = [ /* ... dati precedenti ... */ ];
const digitalProcedures = [ /* ... dati precedenti ... */ ];
---

<html lang="it">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>The Bridge | {name}</title>
    <style>
        :root { --accent: #38bdf8; --bg: #020617; --card: #1e293b; --gold: #fbbf24; --canva: #00c4cc; --tech-bg: #111827; }
        body { background: var(--bg); color: #f1f5f9; font-family: 'Inter', sans-serif; margin: 0; padding: 0; display: flex; flex-direction: column; align-items: center; }
        .container { max-width: 1000px; width: 90%; text-align: center; padding: 4rem 0; }
        
        .tab-bar { display: flex; justify-content: center; gap: 0.5rem; margin: 3rem 0; border-bottom: 1px solid #1e293b; padding-bottom: 1rem; flex-wrap: wrap; }
        .tab-btn { background: none; border: none; color: #94a3b8; font-size: 0.9rem; font-weight: bold; cursor: pointer; padding: 0.5rem 1rem; transition: 0.3s; position: relative; }
        .tab-btn.active { color: var(--accent); }
        .tab-btn.active::after { content: ''; position: absolute; bottom: -1rem; left: 0; width: 100%; height: 2px; background: var(--accent); }

        .content-section { display: none; min-height: 400px; animation: fadeIn 0.4s ease-out; }
        .content-section.active { display: block; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(5px); } to { opacity: 1; transform: translateY(0); } }

        .grid-layout { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 1rem; text-align: left; }
        .info-card { background: #0f172a; border: 1px solid #1e293b; padding: 1.2rem; border-radius: 0.75rem; transition: 0.3s; text-decoration: none; color: inherit; display: block; }
        .info-card:hover { border-color: var(--accent); background: #1e293b; }

        /* Tech Tags Styling */
        .tech-tags { display: flex; flex-wrap: wrap; gap: 0.5rem; margin-top: 1rem; }
        .tag { background: var(--tech-bg); color: var(--accent); font-size: 0.75rem; padding: 4px 10px; border-radius: 20px; border: 1px solid rgba(56, 189, 248, 0.2); }

        .lang-switcher { position: fixed; top: 20px; right: 20px; z-index: 100; display: flex; gap: 5px; }
        .lang-switcher button { background: #1e293b; border: 1px solid var(--accent); color: white; padding: 5px 10px; border-radius: 10px; cursor: pointer; font-size: 0.7rem; }
        
        .nav-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1rem; margin: 2rem 0; }
        .nav-card { background: var(--card); padding: 1rem; border-radius: 0.5rem; text-decoration: none; color: white; border: 1px solid rgba(56, 189, 248, 0.2); transition: 0.3s; }
        
        h1 { font-size: clamp(2.5rem, 8vw, 4rem); margin: 0; letter-spacing: -2px; }
        .badge { font-size: 0.65rem; padding: 2px 6px; border-radius: 4px; border: 1px solid var(--accent); color: var(--accent); }
    </style>
</head>
<body>
    <div class="container">
        <div style="letter-spacing: 4px; font-size: 0.8rem; opacity: 0.6; color: var(--accent);">CARMINE D'ALISE PRESENTS</div>
        <h1>THE BRIDGE</h1>
        
        <div class="nav-grid">
            {quickLinks.map(link => (
                <a href={link.url} target="_blank" class="nav-card">
                    <span style="display:block; font-size: 1.5rem;">{link.icon}</span>
                    <small style="font-size: 0.7rem; font-weight: bold;">{link.name}</small>
                </a>
            ))}
        </div>

        <div class="tab-bar">
            <button class="tab-btn active" onclick="showTab('tech')" id="tab-tech">TECH STACK</button>
            <button class="tab-btn" onclick="showTab('certs')" id="tab-certs">PATH & SKILLS</button>
            <button class="tab-btn" onclick="showTab('sop')" id="tab-sop">SOP & AUDIT</button>
            <button class="tab-btn" onclick="showTab('media')" id="tab-media">CREATIVE MEDIA</button>
        </div>

        <div id="section-tech" class="content-section active">
            <div class="grid-layout">
                {techStack.map(cat => (
                    <div class="info-card" style="border-top: 2px solid var(--accent);">
                        <div style="font-size: 1.5rem; margin-bottom: 0.5rem;">{cat.icon}</div>
                        <h3 style="margin: 0; font-size: 1.1rem; color: #fff;">{cat.category}</h3>
                        <p style="font-size: 0.8rem; opacity: 0.6; margin: 0.3rem 0;">{cat.it}</p>
                        <div class="tech-tags">
                            {cat.items.map(item => (
                                <span class="tag">{item}</span>
                            ))}
                        </div>
                    </div>
                ))}
            </div>
        </div>

        </div>

    <script is:inline>
        function showTab(tabName) {
            document.querySelectorAll('.content-section').forEach(s => s.classList.remove('active'));
            document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
            document.getElementById('section-' + tabName).classList.add('active');
            document.getElementById('tab-' + tabName).classList.add('active');
        }
        // ... setLanguage function ...
    </script>
</body>
</html>